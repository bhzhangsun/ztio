# ── archify 视觉回归产物 ──────────────────────────────────────
# 可再生：archify visual-check <output.html>
# 渲染后的 *.html 是交付物，提交；截图与回执不提交。
*.visual-check.*
*.png

# ── 编辑器 / 系统 ─────────────────────────────────────────────
.DS_Store
._*
*.swp
.idea/
.vscode/

# ── app（Flutter）构建产物 ────────────────────────────────────
app/build/
app/.dart_tool/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/**/build/
app/**/.dart_tool/
app/**/Pods/
app/**/.symlinks/
app/**/GeneratedPluginRegistrant.*
*.iml

# ── server 构建与运行时产物 ───────────────────────────────────
server/data/
server/.env
server/*.tar.gz

# ── 备份归档 ──────────────────────────────────────────────────
# backup.sh 现在默认写到 /var/backups/ztio/（仓库外）。下面两条是纵深防御：
# 万一有人手工往仓库里放备份，或者改了默认路径，这两条仍然挡得住。
#
# 归档里含 identity.secret 与 planet 签名私钥，拿到即可冒充 root。
# 关键：文件名规则（identity.secret 等）**匹配不到 tar.gz 归档** ——
# 这是真实踩过的坑：归档一度出现在 git status 的未跟踪列表里，
# git add -A 就会把私钥推到公开仓库。
backups/
*.tar.gz

# ── 任何时候都不入库 ─────────────────────────────────────────
# 真实地址、密钥、身份文件 —— 本仓库是公开的
identity.secret
authtoken.secret
*.key
*.pem

# ── Python ────────────────────────────────────────────────
__pycache__/
*.pyc
